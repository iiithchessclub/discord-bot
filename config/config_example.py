from os import environ as env

# TIP: can extract options from `env`.

### Bot options
TOKEN = 'BOT_API_TOKEN_HERE'
PREFIX = '-' # bot commands prefix

### Logging
# APP_LOG_DIR = 'directory for logs. defaults to `logs/`'
# BOT_LOG_FILE =  'filename for discord logs, defaults to `discord.log`'
# APP_LOG_FILE = 'filename for application logs, defaults to `app.log`'

### Database
# APP_DATA_DIR = 'directory for databases. defaults to `data/`'
# DEFAULT_DB = 'default sqlite3 database filename, defaults to `default.db`'

### Add custom config options below
